# Activated by SPRING_PROFILES_ACTIVE=prod on the Azure Container App.
# No defaults — the URI must come from the SPRING_DATA_MONGODB_URI env var
# (stored as an Azure secret, pointing to MongoDB Atlas).
spring.config.activate.on-profile=prod

spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI}
