# Default MongoDB database name (the URI in docker-compose.yml overrides this for containers)
spring.data.mongodb.database=vroom

# Using 8081 instead of the default 8080 to avoid conflicts with other local servers
server.port=8081
